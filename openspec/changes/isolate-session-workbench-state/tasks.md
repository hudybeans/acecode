## 1. 会话归属与预览

- [x] 1.1 实现会话工作状态存储、稳定身份和临时归属移交；单测验证隔离、恢复、序列化及迟到更新。
- [x] 1.2 改造文件页签集合、所有页签动作和编辑状态归属；单测覆盖 A 五页签/B 两页签、同名文件、关闭与排序互不影响。
- [x] 1.3 接入预览编辑/滚动恢复和既有三按钮确认；浏览器验证取消、保存失败与跨会话往返。

## 2. 面板与缓存

- [x] 2.1 改造文件树缓存、展开、选中、栏目、滚动与变更列表形式；测试同 cwd 独立恢复及在途请求。
- [x] 2.2 将详情显示、最大化和终端停靠状态按会话维护；浏览器验证 A/B 互不影响。面板宽度按下方全局偏好修正。
- [x] 2.3 隔离 Git 信息、列表、patch 与基线，保留会话内共享及目录变更失效；测试缓存身份与晚到回包。

## 3. 终端与新建交接

- [x] 3.1 PTY 注册表/API 增加 owner 创建、过滤与移交，并更新 daemon-api；C++ 定向测试验证进程保持、过滤和在途创建归属。
- [x] 3.2 控制台接入会话列表和选中项、实例/连接保活与 owner 协议；浏览器及真实 PTY 验证切换不退出进程、关闭互不影响。
- [x] 3.3 新建会话原子移交预览/面板/Git/终端状态，保留聊天草稿回执；验证已有会话不继承、后续新建独立。

## 4. 完整验收

- [x] 4.1 通过完整 pnpm test、pnpm build、C++ 定向构建测试、OpenSpec 严格校验与 git diff --check，记录实际覆盖。
- [x] 4.2 浏览器验证同工作区 A/B、跨工作区、文件/变更/浏览器/终端、三按钮、中文英文及 390px，逐项审计上述范围。

## 5. 全局宽度修正

- [x] 5.1 将 App 的布局宽度恢复为单一全局持久偏好，清除旧会话 layout 记录，保留其他会话状态及现有尺寸约束。
- [x] 5.2 验证会话、工作区、新建入口往返及刷新后宽度一致，旧会话宽度不会覆盖全局值；运行前端测试、构建、OpenSpec 严格校验及差异检查。

2026-09-23 验证：`sessionWorkbench.test.js`、`singleLayout.test.js`、`usePreference.test.js`、完整 `pnpm test`、`pnpm build`、OpenSpec 严格校验及 `git diff --check` 均通过。Chromium 使用当前构建产物和模拟 API 验证了鼠标/键盘调宽、同工作区 A/B 往返、跨工作区、新建入口、刷新持久化及旧 layout 清理；文件页签仍按会话隔离。未重建原生 Desktop 安装包。

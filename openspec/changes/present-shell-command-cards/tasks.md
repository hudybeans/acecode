## 1. 命令数据
- [x] 1.1 提取 Shell 命令展示数据，补齐匹配历史调用的参数，覆盖实时/历史/旧包装/并行/缺失请求测试。

## 2. 卡片
- [x] 2.1 接入共享 Shell 卡片与主题样式，保留工具展开、复制、运行/失败状态和外层消息折叠。

## 3. 验证
- [x] 3.1 完成前端全量测试、构建、严格规范及差异检查；浏览器验证明暗主题、中英文、窄屏、运行/失败、复制、展开及主会话/子代理共享渲染。

## 4. 恢复工具执行行
- [x] 4.1 恢复 Shell 的共用 ActivityLine，仅替换展开详情，保留实时／完成／失败、完整参数、复制和右键展开路径。
- [x] 4.2 验证默认折叠与展开后的执行行、主会话／子代理共享路径、中英文及 390px 布局，完成前端测试、构建和严格规范校验。

## 恢复执行行的验证记录

- 命令展示与工具活动行定向测试通过；完整 `pnpm test` 通过（2586 条通过记录），`pnpm build`、严格 OpenSpec 校验和 `git diff --check` 通过。
- 浏览器覆盖 8 组明暗／中英文／1100px 与 390px 组合，每组验证主会话投影和子代理投影经真实只读 `TranscriptItems` 的共享渲染，共 16 个界面组合。
- 验证默认仅一条带图标、名称、参数的执行行；展开后该行仍在，卡片与行同宽；复制不收起，键盘和桌面事件可展开收起；失败、历史缺参、普通 Read 工具正确；实时输出和完成事件保留展开状态。
- 已核对实际截图。本次未重建或启动原生 Desktop 安装包。

## 初版验证记录（不代表已验证本次修正）

- `pnpm test`：2574 项通过；新增 8 项命令展示与历史配对测试。
- `pnpm build`、`openspec validate present-shell-command-cards --strict`、`git diff --check` 通过；定向样式检测无问题。
- 浏览器验证真实 ToolBlock、主会话与 SubagentPanel 渲染：完整命令恢复、流式更新保留展开状态、复制全文、键盘和桌面菜单事件、失败/空输出，以及原有消息自动折叠均通过。
- 浅色/深色、中英文、390px 窄屏、大字号和减少动画状态通过，无页面横向溢出；截图等待主题过渡结束后保存。
- 此次验证覆盖 Web 源码与构建产物，未重新打包或启动原生 Desktop 安装包。

# 验证记录

2026-09-19，本地源码验证。

- `pnpm test`：完整前端测试通过，包含确认控制器的取消、丢弃、保存、失败重试、重复请求及卸载取消；已有关闭文件和 Modal 契约断言同步更新。
- `pnpm build`：通过，生成单文件前端并通过旧 WebView 正则兼容性检查。
- `pnpm i18n:catalog`：从英文覆盖生成语言目录，按钮为“取消 / 不保存 / 保存”和“Cancel / Don't save / Save”。
- `openspec validate guard-unsaved-file-navigation --strict`、`git diff --check`：通过。

使用独立 Chromium 测试实例加载实际 React 应用，所有 API 和 Desktop bridge 均为测试桩，不操作用户会话和真实文件。已验证：

- 文件树切换：取消后编辑内容、活动页签及树选中项保持；保存完成后切换；丢弃不写磁盘，再次打开恢复基线。
- 会话切换与历史后退：取消不改变原会话和历史目的地；保存失败留在原处；目标会话恢复其独立聊天输入草稿。
- 关闭文件：取消保留文件，丢弃后关闭。
- 工作区与新建会话入口：确认前不调用原生工作区激活；取消保留原会话，丢弃后进入首页。
- 隐藏详情或整个右侧栏：取消后仍显示编辑器。
- 浏览器页签：手动新建和自动前置均等待确认；取消不调用原生选页；保存失败后重试成功才选页。
- 中文、英文与 390×844、减少动态效果：三按钮完整可见，弹窗宽 358px，无内部横向溢出，页面无未捕获异常。

测试在会话导航后等待网络空闲再输入，并断言文字确实进入文件编辑器，避免现有聊天输入框切换后 80ms 自动聚焦影响测试目标。

未重新构建或验证已安装 Desktop 原生包。原生 bridge 的时序通过测试桩验证，真实磁盘保存继续使用既有 read_id 校验接口。

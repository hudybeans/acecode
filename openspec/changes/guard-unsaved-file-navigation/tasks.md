## 1. 统一未保存确认

- [x] 1.1 实现可测试的单请求确认控制器，覆盖取消、不保存、保存成功、保存失败重试、并发请求和卸载取消。
- [x] 1.2 接入预览切换和现有关闭流程，复用文件版本校验保存，按钮统一为“取消 / 不保存 / 保存”；通过定向测试验证保存及丢弃只影响原文件。

## 2. 导航联动

- [x] 2.1 在会话、首页、前进后退及工作区导航执行前接入确认，取消不修改目标或导航历史；覆盖真实导航入口验证。
- [x] 2.2 将浏览器自动前置、手动新建与隐藏详情接入相同检查，确认前保持原活动文件；通过定向验证检查取消与保存失败路径。

## 3. 验证与文案

- [x] 3.1 补齐英文覆盖并生成 i18n 目录，通过完整 `pnpm test`、`pnpm build`、OpenSpec 严格校验及 `git diff --check`。
- [x] 3.2 在浏览器验证三按钮、文件/tab/会话切换、保存失败与关闭回归，检查中文/英文及 390px 展示，并记录实际覆盖范围。

## 1. 交互与状态

- [x] 1.1 将列表展开状态改为独立可见数量，完善五条批次、末批、收起重置和目标批次揭示；通过 sidebarSessions 定向测试。
- [x] 1.2 文件夹行只调用折叠操作、去掉激活样式，接入分批显示并删除控件 hover 背景；通过组件行为和架构回归检查。

## 2. 集成验证

- [x] 2.1 验证文件夹点击保持当前会话、右侧新建按钮、逐批展开及会话顺序、置顶和收起后迟到响应；检查亮暗主题与窄屏控件样式。
- [x] 2.2 运行 pnpm test、pnpm build、OpenSpec strict 验证与 git diff --check，并核对未覆盖原有 Sidebar 改动。

## 验证记录

- 最终前端全量测试 2632 项通过，生产构建和旧 WebView 正则兼容检查通过，OpenSpec strict 与差异检查通过。
- 真实 Sidebar 组件配合模拟 API 的 Chromium 检查覆盖亮暗主题、中英文、1000px/390px 与 reduced-motion；文件夹鼠标及键盘操作、新建按钮、分批展示和无背景 hover 均通过。
- 置顶补偿首批请求、全部收起重开五条、无工作区 5→10→12→5，以及收起后迟到全量响应均通过；每批完整会话 ID 序列断言通过，浏览器未记录 pageerror 或 console error。
- 现有 Sidebar 会话 hover 缓存改动已对照修改前快照保留。未重新打包或验证已安装 Desktop 客户端。

## 1. 配置契约

- [x] 1.1 实现统一配置 Schema、诊断、严格解析/序列化与项目覆盖；通过 Schema 和覆盖规则单元测试。
- [x] 1.2 按范围保存有效快照与启动恢复，接入通用配置读写；验证无效写入、快照失败撤销与仅恢复 MCP 段。

## 2. 运行时与编辑入口

- [x] 2.1 接入项目运行时 owner、会话/headless/TUI 能力解析；验证同名隔离、专家保留与过期连接丢弃。
- [x] 2.2 MCP HTTP 读写、开关、重新加载接入统一服务，提供 Schema 发现；验证认证、校验、凭证保留、保存失败和项目 API。
- [x] 2.3 TUI 和通用文件编辑在修改前校验；验证 Write/Edit/Patch 拒绝，并编译 TUI 集成。
- [x] 2.4 设置增加公共/项目选择与 Schema 诊断，沿用样式与国际化；前端完整测试、目录生成和生产构建通过。

## 3. 交付验证

- [x] 3.1 文档说明覆盖、Schema、恢复及 API 响应；通过严格 OpenSpec 校验与差异检查。
- [x] 3.2 运行 C++ 定向集成测试和前端检查，审查最终变更，记录验证范围和限制；详见 validation.md。

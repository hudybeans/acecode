## 1. 探索与复现

- [x] 1.1 完成 Windows Chromium 键鼠基线探索，记录 tag 单选、双向拖选、Shift 选择、剪贴板与历史问题和可复现步骤。

## 2. 修复与回归

- [x] 2.1 统一 tag 选择和混合编辑边界，浏览器验证文字与全部 tag 类型的选中、剪切、替换、删除以及附件动作隔离。
- [x] 2.2 修复自动命令转换历史，定向测试证明 Ctrl+Z/Ctrl+Y 有效且附件与选区不丢失。
- [x] 2.3 添加可重复执行的浏览器回归脚本，覆盖鼠标、键盘、剪贴板、多行及中英文窄屏样式。

## 3. 验证与交付

- [x] 3.1 运行 pnpm test、pnpm build、OpenSpec 严格验证、git diff --check 和 impeccable 检测；记录测试结果与 Windows 客户端验证边界。

## 4. 按确认稿修正选择视觉

- [x] 4.1 保留 tag 本体的圆角、底色、边框和图标颜色，以独立矩形选区背景连接文字与 tag；取消选中只移除选区背景。
- [x] 4.2 放大前置图标以匹配文字，修正 tag 左右间距，检查各类 tag 和字号下的对齐。
- [x] 4.3 验证真实拖选、Shift 多选、跨行与取消选择，检查明暗主题和 390px 截图，完成前端测试、构建和 OpenSpec 检查。

## 5. 拖入文件后继续输入

- [x] 5.1 在 Windows WebView2 隔离窗口复现进入时激活失败后，文件 tag 和 DOM 光标正常但系统按键仍送往来源窗口的问题。
- [x] 5.2 文件释放并被接收时恢复一次原生窗口焦点，保留现有光标与完整路径，不在异步解析或上传完成时抢焦点。
- [x] 5.3 验证原生拖放后直接输入、方向键与中文输入，覆盖无效拖放和异步处理期间切换窗口，完成前端测试与构建。

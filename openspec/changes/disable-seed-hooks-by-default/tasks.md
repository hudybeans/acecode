## 1. Runtime and seed defaults

- [x] 1.1 Honor disabled Codex-shaped sources and defer payload serialization until a runnable command; verify source and inactive-dispatch regression tests.
- [x] 1.2 Disable the bundled hook and advance revision/fingerprints with prior official migration; verify canonical hashes and fresh/upgrade/preservation seeder tests.

## 2. Documentation and verification

- [x] 2.1 Document disabled seed defaults and manual reviewed opt-in; verify example configuration remains active and matches the seed handlers.
- [x] 2.2 Run focused hook/seeder tests, strict OpenSpec validation and scoped diff checks; record outcomes and any validation limits.

## 1. 合并加载反馈

- [x] 1.1 归并记录加载和运行环境恢复状态，将唯一遮罩挂到会话主列；用定向测试验证完成顺序、失败优先级、只读会话和 JSX 定位祖先。

## 2. 集成验证

- [x] 2.1 使用真实加载组件及现有样式在浏览器验证宽窄屏、中英文、侧面板、输入区高度变化和阶段切换时居中，记录验证结果。
- [x] 2.2 通过 `pnpm test`、`pnpm build`、OpenSpec 严格校验和 `git diff --check`，核对已有无关改动保留并记录验证边界。

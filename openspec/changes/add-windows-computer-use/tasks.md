## 1. 设置与能力开关

- [x] 1.1 增加默认关闭配置和 Windows 能力 API，配置解析、持久化及路由测试通过。
- [x] 1.2 增加中英文内置工具设置行，验证加载、切换、失败、非 Windows 禁用与窄屏显示。
- [x] 1.3 接通动态工具注册与执行时开关校验，测试关闭后的旧调用被拒绝。

## 2. Windows 原生执行

- [x] 2.1 实现窗口/应用发现、启动、UIA 观察和窗口截图，通过原生编译与受控窗口截图验证。
- [x] 2.2 实现全部输入和控件动作、观察过期校验，通过参数与受控窗口动作验证。
  - 前台限制已解除。原生直连及生产 broker/helper 链路均通过真实赋值、点击、焦点、组合键、中文与补充平面字符、滚动、画布对象拖动和 OLE 拖放验证；Toggle、Select、Expand、Collapse、Invoke 均核对实际控件变化。原生 EDIT 不支持 Ctrl+A，fixture 使用其支持的导航组合键并先验证选择范围。
- [x] 2.3 实现独立执行器、会话租约、超时取消和回合释放，通过协议、撤销及资源释放测试。
- [x] 2.4 接入构建与安装，验证 helper 与主程序产物及安装路径。
  - MSVC MinSizeRel 的主程序、桌面端和 helper 已构建；独立安装组件交付路径及 helper SHA256 一致性已核对。

## 3. 模型观察闭环

- [x] 3.1 接通 OpenAI 兼容与 Anthropic 图片工具结果，测试多结果顺序、图片数据和非视觉模型分支。
- [x] 3.2 为模型提供动作约束和再观察指引，验证工具 schema、附件及权限模式集成。

## 4. 集成验证与说明

- [x] 4.1 更新 API 与功能说明，记录 Windows 权限、截图回退和模型能力边界。
- [x] 4.2 运行相关原生测试、pnpm test、pnpm build、OpenSpec strict 验证和 git diff --check，审查无关工作保留情况。
  - 最新 117 项关联测试通过，涵盖多图回传、坐标映射和实时开关；此前 246 项 AgentLoop/权限回归、原生观察与 broker 集成测试、前端测试/构建、中英文窄屏交互检查、打包脚本测试均通过。
  - 原有无关改动按基线保留，共用文件仅合并本功能必要变更。真实输入和实际 helper 链路已通过；本地 MSVC 构建通过，WinLibs 构建因既有损坏头文件未完成。本任务没有发布安装包或替换已安装客户端。

## 5. Codex 能力对照补齐

- [x] 5.1 合并已安装应用与运行窗口身份，保证同一 app id 启动后可查到窗口，并使可聚焦控件正确广告 focus/raise。
- [x] 5.2 补齐 Codex 支持的按键别名和数字键盘 Enter 语义，验证键码、扩展标记与无效组合。
- [x] 5.3 超大观察输出经实际工具结果落盘后仍保留观察标识、窗口与截图坐标信息。
  - 新增身份、按键和真实工具结果持久化回归；本轮 51 项相关测试全部通过。
- [x] 5.4 元素点击验证真实可点击点及命中元素，避免同一窗口浮层遮挡时点击错误控件。
  - 原生受控窗口实测：正常按钮解析可点击点成功，同窗口兄弟控件完全覆盖后返回 obscured；测试不发送输入。
- [x] 5.5 补齐 get_window 窗口恢复与相关瞬态界面截图，验证对应截图坐标和窗口归属。
  - get_window、最多四个 surface、逐图元数据、输入路由和部分截图失败处理已实现；最新 117 项集成回归通过。原生 fixture 已验证同 app 恢复/不同 app 拒绝、owned popup、标准菜单与 ComboBox 画面、PNG 尺寸、同线程无关窗口排除、移动/关闭失效、多图拒绝隐式目标。8 项坐标变换测试通过；真实 screenshot_id 坐标点击已成功选择菜单和 ComboBox 项目。
- [x] 5.6 补齐 selected_elements 和非焦点文档的 document_text，保留密码与大小边界。
  - 文档优先级/密码规则单测通过；原生 fixture 的真实 ListBox 选中条目出现在 selected_elements。
- [x] 5.7 验证拖拽对实际拖放控件和画布的效果、按键/鼠标动作及显示器坐标映射；不能仅以收到鼠标移动事件判定拖放完成。
  - 真实 helper 链路已验证画布对象最终位置、滚轮改变内容位置、DoDragDrop/IDropTarget 的 DRAGDROP_S_DROP 与目标 payload，以及菜单/ComboBox 项目选择。全部可用显示器（本机 1 台）和 3000 像素窗口缩放后的实际坐标点击通过；负原点、逐图缩放、边界和溢出由 8 项坐标单测覆盖，未声称多显示器硬件实测。

## 6. 设置对齐与指针可见性修正

- [x] 6.1 移除电脑操控行多余的缩进，与完整内置工具列表中的图标、文字、右侧操作及分隔线对齐，验证中英文、明暗色和窄屏。
  - 真实 SectionTools 组合检查通过：390/1280px、中英文、明暗主题共 8 组，包含保存失败、重试、禁用和非 Windows 状态；已复现并移除原先额外的 14px 缩进。
- [x] 6.2 对照 Codex 的可见操控反馈，补齐 Windows 指针显示；截图路径统一处理指针位置、热点和缩放，不能影响目标命中或截取无关窗口。
  - 已实现 helper 原生可见指针、点击/移动反馈、统一截图合成及附件元数据保留；真实 broker/helper 完整输入检查通过，覆盖本机两个可用显示器、3000 像素窗口缩放与指针热点一致性。已查看实际 PNG，确认不是仅返回坐标或工具成功状态。
- [x] 6.3 补充真实指针可见性与完整设置列表验证，运行相关测试、前端构建、原生构建和 OpenSpec 检查。
  - 最终 131 项关联测试通过，包含 8 项像素合成、4 项真实指针窗口、2 项附件落盘/模型回传新回归；完整生产 broker/helper 输入实测通过，确认指针可见、坐标一致、不抢焦点、不进入窗口列表及 release 后隐藏。
  - pnpm test、pnpm build、MSVC MinSizeRel 的主程序/Desktop/helper/原生 fixture 构建、OpenSpec strict 和 git diff --check 均通过。8 组完整设置列表截图已核对实际字体和几何。当前运行的 build/Release 进程未替换，本次新版位于 build/MinSizeRel。

## 7. 实验性标注与主题指针配置

- [x] 7.1 增加实验性标注、行内配置展开、两种主题色指针预览和持久化选择，维持完整工具列表对齐及中英文窄屏可用性。
  - 390/1280px、中英文、明暗主题共 8 组完整 SectionTools 验证通过，涵盖键盘选择、关闭时配置、保存失败保留、重试及连续主题变更；图标、文字与最右开关对齐，无横向溢出。
- [x] 7.2 扩展配置/API/runtime 外观传递，跟随当前主题色且不隐式启用工具，覆盖保存失败、旧配置和非法样式。
  - 配置与真实 HTTP 路由验证部分更新、关闭时保存、旧配置默认、颜色规范化和保存失败不改变运行时；broker fixture 验证模型无法覆盖外观、helper PID/租约/观察连续性。应用外层同步实时主题，重连/回到前台刷新，首次读取失败后的重试保留最新主题色。
- [x] 7.3 原生绘制主题色箭头及可选右下角 ACE，保持点击反馈、热点、DPI、截图与穿透行为一致，验证实际图片和 helper 操控。
  - 生产 broker/helper 的完整操控实测通过，包含默认蓝色、指定橙色、ACE/plain 的 PNG 像素与相同热点；已查看实际截图。本次可用的一台显示器及 3000px 缩放窗口坐标点击通过。
- [x] 7.4 更新功能/API说明，完成关联测试、整页视觉验证、前后端构建及 OpenSpec strict 检查。
  - 139 项 C++ 关联测试、原生 helper 完整操控与 broker 集成测试、pnpm test/build、8 组完整页面验证全部通过。MSVC MinSizeRel 的主程序、Desktop、helper 已构建，CMake 在最终前端构建后重新配置并嵌入最新页面。OpenSpec strict 与 git diff --check 通过；当前运行的 build/Release 进程未替换，本次新版位于 build/MinSizeRel。

## 8. Windows 执行器全量发布集成

- [x] 8.1 验证 GitHub Windows x64/ARM64 构建、ZIP、安装器与 npm 包均交付独立执行器，非 Windows 不要求该文件。
  - Windows CMake 依赖构建并安装独立组件；GitHub ZIP 解压后核对 helper SHA256，ARM64 PE 架构包含 helper。5 项 npm 包测试、10 项本地包结构测试通过，涵盖缺失、空文件及非 Windows 不交付。
- [x] 8.2 将全平台更新镜像门禁同步到仓库发布脚本，验证 Windows 执行器缺失或空文件时在更新发布前失败，保留六平台 ZIP 与双 macOS PKG 门禁。
  - 仓库发布脚本补齐全平台 mirror，0.9.22 起 Windows 更新包强制要求非空 helper；本地 Windows 包同时交付 WhatsApp 桥接资源。快速发布脚本与完整镜像脚本回归通过，缺失或空 helper 不改更新清单/公共文件，历史旧版本仍可镜像修复。
- [x] 8.3 汇总已结束会话的可用本地成果，完成远端 PR 审核、修复、测试与合并，精确提交并推送。
  - 已保留并整合完成会话的本地成果，PR #62/#63/#64 修复后合并。启动器 54 项脚本测试、侧栏 14 项真实回调时序回归、合并后前端全量测试及构建通过；Windows 主程序/Desktop/helper 构建、138 项日志与电脑操控测试及真实多进程日志追加 CTest 通过。发布脚本补齐 ZIP/PKG MIME 与公共清单逐包校验，PowerShell 5.1 的快速/全量镜像回归通过。
- [x] 8.4 发布下一全量版本，核对最终提交、GitHub 产物、六平台更新清单、双 macOS PKG 及公共下载哈希。
  - v0.9.22 发布提交为 362a7ccd68177985afe81c23e41e038a700fc38c；GitHub package run 35468311496 成功，正式 Release 交付 37 个资产。发布提交 test run 35468309485 全部成功，其中 Linux CTest 4681 项零失败，包含真实多进程日志追加回归。
  - 全量镜像脚本成功退出，aupdate latest=0.9.22；六平台更新 ZIP、两个 macOS PKG 及 PKG 固定别名完成公网 MIME、长度和 SHA256 校验，清单逐包字段及升级说明一致。正式 Windows x64/ARM64 ZIP 内三个可执行文件均通过 PE 架构检查，独立 helper 完整交付，x64 包内 CLI 实际报告 acecode v0.9.22。

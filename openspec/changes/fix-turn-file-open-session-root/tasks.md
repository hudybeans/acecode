## 1. 校正会话归属

- [x] 1.1 工作区恢复接口拒绝元数据缺失及无工作区会话；扩展 Web smoke test，验证已运行的无工作区会话也返回 404 且原目录不变。

## 2. 修正桌面跳转

- [x] 2.1 错误工作区提示返回 404 时仅对真实无工作区会话回退恢复，并以规范 ref 构造跳转 URL；通过定向 JS 测试验证 `pelican-bicycle.html` 的预览根目录和其他错误分支。

## 3. 集成检查

- [x] 3.1 运行 Web 测试及构建、可用的定向 C++ smoke test、OpenSpec 严格验证和 `git diff --check`，复查变更范围与结果。

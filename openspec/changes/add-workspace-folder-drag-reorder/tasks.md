## 1. 顺序持久化

- [x] 1.1 注册表原子保存并恢复工作区顺序，支持隐藏/新增工作区；通过注册表测试验证重建恢复、非法排列及写失败。
- [x] 1.2 接入排序 API 并更新 daemon-api 文档；通过路由验证覆盖成功、格式错误和冲突。

## 2. 侧栏交互

- [x] 2.1 增加排序纯函数、乐观保存和轮询竞争保护；定向测试验证边界、刷新及回滚。
- [x] 2.2 接入标题拖动、文件夹浮层、分组插入线、自动滚动和取消/点击抑制；浏览器验证展开与收起分组、控件隔离和列表外释放。
- [x] 2.3 接入 Alt+方向键、主题 token 和中英文文案；浏览器验证键盘、390px、中英文及减少动态效果。

## 3. 综合验证

- [x] 3.1 运行前端全量测试、构建、相关后端测试、OpenSpec strict 校验和差异检查，核对无关原有修改未被覆盖并记录验证边界。

## 验证记录

- `pnpm test`、`pnpm build` 和构建产物正则兼容检查通过；新增文案由 `pnpm i18n:catalog` 生成。
- Release 单测目标构建通过，44 项工作区相关测试通过；统一 API 错误字段后再次增量构建并运行新增真实 HTTP 测试，400/409/500 错误码和持久化行为通过。
- 真实 Sidebar 组件在 Chromium 中 14 组检查通过，覆盖分组拖动、浮层位置与字体、占位、刷新恢复、取消、按钮隔离、键盘、自动滚动、保存失败和冲突即时刷新，以及 390px、中英文、深浅主题和减少动态效果。浏览器网络使用隔离模拟数据，后端接口另由真实 HTTP 单测验证。
- OpenSpec strict 校验、定向 `git diff --check` 通过。无关原有文件按哈希核对；`ComputerUseSettings.jsx` 在任务期间另有更新，本变更未编辑该文件，其余无关文件与快照一致。
- 尚未重建或安装 Desktop/daemon 可执行文件；本次验证不代表安装包已更新。
